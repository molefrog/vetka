{
  "buildCommand": "bun run build",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "has": [{ "type": "host", "value": "(?<sub>[^.]+)\\.vetka\\.sh" }],
      "destination": "/api/serve/$1"
    }
  ]
}
